# python-challenge
Morales- Module 3 Homework Assignment

There are two assignments here: PyPoll and PyBank

Inside the PyPoll folder, there is a python script "main.py", which pulls from a csv in the Resources folder. Upon running "main.py", the outputs print to the terminal and the "output.txt" file.

Inside the PyBank folder, there is a python script "main.py", which pulls from a csv in the Resources folder. Upon running "main.py", the outputs print to the terminal and the "output.txt" file.
